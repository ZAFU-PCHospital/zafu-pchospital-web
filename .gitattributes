# 统一换行符：仓库内一律使用 LF，避免 Windows / macOS 成员之间产生无意义 diff
* text=auto eol=lf

# 二进制文件：不做换行符转换，也不参与文本 diff
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary

# 锁文件：由包管理器生成，不参与合并冲突的人工解决
pnpm-lock.yaml -diff merge=ours
package-lock.json -diff
